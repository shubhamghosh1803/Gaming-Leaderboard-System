# Backend API Base URL (Teammate's MySQL/ODBC server endpoint)
VITE_API_BASE_URL=http://localhost:5000/api

# Set to "false" to connect directly to the live backend API, or "true" for standalone frontend mock mode
VITE_USE_MOCK=true
